{
  "source": "index.html",
  "scripts": {
    "clean:output": "rimraf dist",
    "start": "yarn clean:output && parcel",
    "build": "yarn clean:output && parcel build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "parcel": "^2.2.1",
    "postcss": "^8.4.5",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.0.15"
  }
}
